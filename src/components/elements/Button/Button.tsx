import { Wrapper } from "./style";

export default function Button() {
    return (
        <Wrapper>
            <button onClick={e => ('')} className="text">Subsctibe</button>
        </Wrapper>
    )
}